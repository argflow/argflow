GRAPH_FILENAME = "graph.json"
MODEL_DIRNAME = "model"

# Indexed only on models and explanations for now, would need to change this if the
# application was meant to be multi-user.
cached_explanations = {}
